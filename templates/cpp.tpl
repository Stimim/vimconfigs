#include <iostream>
#include <vector>
#include <cstdio>
#include <map>
#include <set>
#include <algorithm>

typedef long long LL;
typedef unsigned long long ULL;

#define RI(x) scanf("%d", &(x))
#define RLL(x) scanf("%lld", &(x))
#define RULL(x) scanf("%llu", &(x))
#define RNI(n, i, p) \
  do { for (int i = 0; i < n; ++ i) scanf("%d", &p[i]); } while (0)
#define RNLL(n, i, p) \
  do { for (int i = 0; i < n; ++ i) scanf("%lld", &p[i]); } while (0)
#define RNULL(n, i, p) \
  do { for (int i = 0; i < n; ++ i) scanf("%llu", &p[i]); } while (0)

using namespace std;

int main(int argc, char* argv[]) {
  return 0;
}
